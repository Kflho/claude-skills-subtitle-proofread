[pytest]
testpaths = tests
pythonpath = scripts
python_files = test_*.py
addopts = -v --tb=short
markers =
    slow: tests that need network or large files
    tier3: tests that need mocked external binaries (Whisper, FFmpeg, LLM)
